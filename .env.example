# =============================================================================
# Desarrollo LOCAL (recomendado): API + Postgres en tu máquina
# =============================================================================
VITE_API_URL=http://localhost:3000/api/v1
VITE_API_PROXY_TARGET=http://localhost:3000

# Alternativa: URL relativa + proxy de Vite (mismo destino que VITE_API_PROXY_TARGET)
# VITE_API_URL=/api/v1

# Producción (solo build/deploy; no uses esto en local)
# VITE_API_URL=https://ffcore-api.onrender.com/api/v1
# VITE_API_PROXY_TARGET=https://ffcore-api.onrender.com
