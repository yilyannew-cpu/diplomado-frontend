# Desarrollo: URL relativa → Vite hace proxy a Render (evita errores CORS en localhost)
VITE_API_URL=/api/v1

# Destino del proxy (solo vite.config.ts; no hace falta cambiarla)
# VITE_API_PROXY_TARGET=https://ffcore-api.onrender.com
