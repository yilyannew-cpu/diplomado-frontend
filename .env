# =============================================================================
# Desarrollo LOCAL — API en tu PC (Postgres Docker), NO Neon
# Tras cambiar esto: para el front (Ctrl+C) y vuelve a correr npm run dev
# =============================================================================
VITE_API_URL=http://localhost:3000/api/v1

# Destino del proxy de Vite
VITE_API_PROXY_TARGET=http://localhost:3000

# --- Producción / Render (NO uses esto en local) ---
# VITE_API_URL=https://ffcore-api.onrender.com/api/v1
# VITE_API_PROXY_TARGET=https://ffcore-api.onrender.com
